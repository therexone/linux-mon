import 'package:flutter/material.dart';

class DiskPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Disk Page'),),
    );
  }
}